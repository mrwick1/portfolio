# Work Log — 2026-09-04

## ✅ Move TradersConnect demo out of article slug to /demos/tradersconnect/

**Repo:** portfolio · **Branch:** chore/p8-demo-url · **Commit:** `b5df484 Move the TradersConnect demo out of the article slug`

### Symptom

Opening `COMMIT_EDITMSG` records the motivation: the article at `/writing/tradersconnect-rework.html` and the reworked landing page demo at `/writing/tradersconnect-rework/` were two unrelated pages separated only by a trailing slash, creating URL ambiguity between the case study article and the interactive demo.

### Fix

1. Moved `writing/tradersconnect-rework/` to `demos/tradersconnect/`.
2. Repointed 11 image references in `writing/tradersconnect-rework.html` to `../demos/tradersconnect/images/` and updated the reworked demo button link to `https://www.arjunp.pro/demos/tradersconnect/`.
3. Added 301 redirects in `vercel.json` for both `/writing/tradersconnect-rework/` and `/writing/tradersconnect-rework` targeting `/demos/tradersconnect/`.
4. Added `<loc>https://www.arjunp.pro/demos/tradersconnect/</loc>` (`lastmod` `2026-09-04`, `priority` `0.6`) to `sitemap.xml`.
5. Added `check-links.py` (90 lines) to assert that the old path is gone, new directory and 8 `.webp` images exist, old paths are absent from the article, redirects and sitemap entries exist, and all local `href` and `src` links resolve across the site's HTML files.

### Verification

Opening `check-links.py` defines the automated checks:
- Verifies `writing/tradersconnect-rework` is removed and `demos/tradersconnect/index.html` and `demos/tradersconnect/images` exist.
- Counts 8 `.webp` image assets in `demos/tradersconnect/images`.
- Verifies absence of `tradersconnect-rework/images` in `writing/tradersconnect-rework.html`.
- Verifies 301 redirects in `vercel.json` for both trailing and non-trailing slash paths.
- Verifies presence of `https://www.arjunp.pro/demos/tradersconnect/` in `sitemap.xml`.
- Traverses and resolves all local relative and root-relative `href`/`src` links in `writing/tradersconnect-rework.html`, `demos/tradersconnect/index.html`, `index.html`, and `writing/index.html`.

### Unknowns

Session transcript was not recorded (`Newest session transcript: none found`). Specific mid-session alternatives or intermediate test failures prior to commit `b5df484` are unknown.
