# Work Log — 2026-09-05

## ✅ Lead homepage with strongest work and restructure projects

**Repo:** portfolio · **Branch:** feat/p14-homepage-restructure · **Commit:** `7eeaf78 Lead the homepage with the strongest work and drop the client section`

### Symptom

Opening `COMMIT_EDITMSG` records the motivation: the homepage hierarchy was not structured for founders evaluating a founding engineer. Experience preceded projects, and a standalone Client Work section ran parallel to full-time experience, giving the false impression of concurrent employment rather than client engagements.

### Fix

1. Opening `COMMIT_EDITMSG` and `index.html` shows Selected Work (`#projects`, numbered `01`) moved before Experience (`#experience`, numbered `02`) so evidence of shipped work leads the page.
2. Promoted three projects into the featured row (`project-card--featured`): Voice AI Platform (01), EdTech Assessment Platform (02), and Suzuki Connect RE (03) to demonstrate sole ownership, full-stack breadth (web, mobile, AI), and depth. Moved Astra UI (07) out of the featured row.
3. Merged the five client engagements into the Projects grid as client-project cards: Commercial real-estate platform (05), AI product configurator (06), Employee intranet (09), Salesforce UI modernisation (12), and Drupal 10 theme build (15), aligning page presentation with the resume.
4. Added inline `TODO(arjun)` comments on each of the five client cards in `index.html` to track missing measurable outcome metrics (brokerage lead generation/publish time, e-commerce conversion/support load, intranet adoption, Salesforce design-token maintenance savings, and whether to retain or cut the Drupal 10 card).
5. Rewrote the hero bio in `index.html` (lines 1221–1240) to emphasize taking products from conversation to shipped, expanded the focus metadata to `Web · Mobile · Backend · AI`, and set the status line to `Open to founding-engineer roles`.
6. Renumbered sections across `index.html` (Projects as `01`, Experience as `02`, Technical Skills as `03`) and updated primary navigation links in `<nav>` accordingly.

### Verification

Opening `index.html` confirms:
- Primary navigation links in `<nav>` order `#projects` before `#experience`.
- Hero metadata displays Focus as `Web · Mobile · Backend · AI` and Status as `Open to founding-engineer roles`.
- Selected Work section is numbered `01` containing 15 project cards, led by 3 featured cards (Voice AI Platform, EdTech Assessment Platform, Suzuki Connect RE).
- Five client project cards (05, 06, 09, 12, 15) contain `TODO(arjun)` outcome tracking annotations.
- Experience section is renumbered as `02` under the `Work History` section label.

### Unknowns

Session transcript was not recorded (`Newest session transcript: none found`). Specific intermediate copy variations, layout iterations, or discarded project card rankings explored prior to commit `7eeaf78` are unknown.
